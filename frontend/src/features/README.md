# Feature modules

Tạo module theo cấu trúc gợi ý:

```text
features/<module>/
├── components/
├── hooks/
├── schemas/
├── services/
└── types/
```

Các module dự kiến: `auth`, `categories`, `recipes`, `search`. Chỉ tạo thư mục con khi có file thực tế để tránh cây thư mục rỗng.

