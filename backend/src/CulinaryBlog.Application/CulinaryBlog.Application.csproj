<Project Sdk="Microsoft.NET.Sdk">
  <ItemGroup>
    <ProjectReference Include="../CulinaryBlog.Domain/CulinaryBlog.Domain.csproj" />
  </ItemGroup>
  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.DependencyInjection.Abstractions" />
    <!-- Chỉ để IApplicationDbContext expose được DbSet<T> — mẫu hình chuẩn của
         Clean Architecture (Jason Taylor template), không phá vỡ Dependency Rule
         vì đây vẫn là abstraction (interface), không phải implementation EF Core. -->
    <PackageReference Include="Microsoft.EntityFrameworkCore" />
  </ItemGroup>
</Project>
